*-kubeconfig
*-kubeconfig.crypt.yaml

# Terraform provider cache: ~34 MB of platform-specific (darwin_arm64) binaries.
# Regenerated by `terraform init`. The .terraform.lock.hcl beside it SHOULD be
# committed - it pins the provider versions.
.terraform/
terraform.tfstate
terraform.tfstate.backup
.terraform.lock.hcl